package org.neidysvelasquez.claims_management_system;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Test class for verifying the Claims Management System application's context.
 * Ensures that the application starts and loads its context correctly.
 */
@SpringBootTest
class ClaimsManagementSystemApplicationTests {

    /**
     * Tests whether the Spring application context loads successfully.
     */
    @Test
    void contextLoads() {
        // This test passes if the application context loads without exceptions.
    }
}

